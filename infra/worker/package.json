{
  "name": "ai-dev-annotator-licenses",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that receives Stripe webhooks and issues Ed25519-signed AI Website Dev Annotator license keys.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
