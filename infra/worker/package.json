{
  "name": "ai-dev-annotator-licenses",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that receives Stripe webhooks for AI Website Dev Annotator and emails Ed25519-signed license keys via Resend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
